server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: health,info,env
  endpoint:
    health:
      show-details: always

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cash_db
    username: root
    password: root
  sql:
    init:
      mode: always
  output:
    ansi:
      enabled: ALWAYS
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:9090/realms/mba
      client:
        provider:
          keycloak:
            issuer-uri: http://keycloak:9090/realms/mba # Адрес сервера авторизации для получения токена
        registration:
          mba-cash: # Описание клиента OAuth2
            provider: keycloak
            authorizationGrantType: client_credentials
            client-id: mba-cash
            client-secret: TyMXeT5voxlckzdKtmEY2u2QyCWqgjct
            scope: profile
            clientAuthenticationMethod: client_secret_post # Способ передачи секрета